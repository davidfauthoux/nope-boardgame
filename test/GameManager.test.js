
describe("Verify GameManager functions", () => {

  test("Check createPool()", () => {
    //const gm = new GameManager(null);
    //expect(gm.parseSpotLocation("01-0")).toStrictEqual({"i": -1, "j": 0});
  });
  test("Check createTrack()", () => {
    //const gm = new GameManager(null);
    //expect(gm.parseSpotLocation("01-0")).toStrictEqual({"i": -1, "j": 0});
  });
  test("Check createGrid()", () => {
    //const gm = new GameManager(null);
    //expect(gm.parseSpotLocation("01-0")).toStrictEqual({"i": -1, "j": 0});
  });
  test("Check newPool()", () => {
    //const gm = new GameManager(null);
    //expect(gm.parseSpotLocation("01-0")).toStrictEqual({"i": -1, "j": 0});
  });
  test("Check clear()", () => {
    //const gm = new GameManager(null);
    //expect(gm.parseSpotLocation("01-0")).toStrictEqual({"i": -1, "j": 0});
  });

});